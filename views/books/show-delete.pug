extends ../layout

block content
  h2= book.title
  include by_line
  p= book.author
  a(href="../") &larr; Back 
  | | 
  a(href="/books/" + book.id + "/edit") Edit book 
  | | 
  a(href="/books/" + book.id + "/delete") Delete book